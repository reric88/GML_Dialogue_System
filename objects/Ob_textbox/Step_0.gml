if(keyboard_check_pressed(vk_space)) {
	// Only increase page if page+1 is less than total entries
	if(page+1 < array_length(text)) {
		page += 1;	
		charcount = 0;
		if(charcount=0){
	} else {
	instance_destroy();
	master.alarm[1] = 1;
	}
	}
}